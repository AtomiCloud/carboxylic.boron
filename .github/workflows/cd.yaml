name: CD

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  publish:
    name: Publish NuGet Package
    uses: AtomiCloud/ketone.nuget/.github/reusable/cd.yaml@main
    with:
      atomi_platform: carboxylic
      atomi_service: boron
      target: Boron
