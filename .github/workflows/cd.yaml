name: CD

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  publish:
    name: Publish NuGet Package
    uses: AtomiCloud/ketone.nuget/.github/workflows/reusable-cd.yaml@main
    secrets: inherit
    with:
      atomi_platform: carboxylic
      atomi_service: boron
      target: Boron
